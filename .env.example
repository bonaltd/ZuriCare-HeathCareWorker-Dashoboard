# Database (MariaDB/MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=zuricare

# API server
PORT=3001

# Frontend (optional - defaults to /api with Vite proxy)
# VITE_API_URL=http://localhost:3001/api

# App URL (for invite links in emails)
APP_URL=http://localhost:5173

# SMTP (for sending invite emails). If not set, dev uses Ethereal test inbox.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@zuricare.org
